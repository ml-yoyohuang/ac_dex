$sprite
  display inline-block
  background url('~img/{{{spritesheet.escaped_image}}}') no-repeat
  
{{#sprites}}
.spr-{{parseName name}}
  width {{px.width}}
  height {{px.height}}
  @extends $sprite
  background-position {{px.offset_x}} {{px.offset_y}}
  &.style-auto
    width 100%
    height auto
    background-position {{bgPosition total_width width offset_x}} {{bgPosition total_height height offset_y}}
    background-size {{percent total_width width}} auto
    &:before
      content ''
      display block
      padding-top {{percent height width}}
{{/sprites}}